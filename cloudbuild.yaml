steps:
  # Build image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/eazyslot"
      - "."

  # Push image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/eazyslot"

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - eazyslot
      - --image
      - gcr.io/$PROJECT_ID/eazyslot
      - --region
      - us-central1
      - --platform
      - managed
      - --allow-unauthenticated
      - --port
      - "8080"
      - --memory
      - "1Gi"
      - --cpu
      - "1"
      - --max-instances
      - "1"
      - --timeout
      - "3600"

images:
  - "gcr.io/$PROJECT_ID/eazyslot"

options:
  logging: CLOUD_LOGGING_ONLY
