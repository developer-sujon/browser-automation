version: '3.8'

services:
  app:
    build: .
    restart: always
    ports:
      - "4000:4000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    # If you want to scale horizontally (e.g., 3 instances):
    # deploy:
    #   replicas: 3
